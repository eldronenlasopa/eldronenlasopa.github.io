import React from 'react';

/**
 * Dark marketing footer: brand blurb, link columns, fine print.
 * @startingPoint section="Navigation" subtitle="Footer oscuro con columnas de enlaces" viewport="1000x360"
 */
export interface FooterProps {
  style?: React.CSSProperties;
}
export function Footer(props: FooterProps): JSX.Element;

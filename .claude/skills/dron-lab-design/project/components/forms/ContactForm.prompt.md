# ContactForm

Ready-made lead-capture form composing Input, Select, Textarea and Button — the standard "¿Hablamos de tu proyecto?" block used across marketing surfaces. Cosmetic by default; wire `onSubmit`.

```jsx
<ContactForm onSubmit={() => console.log('enviado')} />
<ContactForm title="Agenda una demo" subtitle="Te contactamos hoy mismo." />
```

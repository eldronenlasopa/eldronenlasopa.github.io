# Header

Marketing-site top navigation: wordmark on the left, nav links center/right, primary CTA. Light variant is glassy (blurred translucent white); `inverse` is solid dark for hero overlays.

```jsx
<Header cta="Solicitar propuesta" onCta={()=>{}} />
<Header inverse links={[{label:'Servicios',href:'#'}]} />
```
